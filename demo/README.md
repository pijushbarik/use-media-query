# use-media-query Demo React App

Install the packages
```sh
npm install
```

Run dev server
```sh
npm run dev
```

Make a production build
```sh
npm run build
```

Production bundle will be stored in `dist` directory.